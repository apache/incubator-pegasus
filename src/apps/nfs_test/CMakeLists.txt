set(DSN_APP_TARGET "dsn.nfs.test")
set(DSN_EXTRA_LIBS dsn.nfs)
file(GLOB BINPLACE_FILES "${CMAKE_CURRENT_SOURCE_DIR}/config.ini")
include_directories(AFTER ../nfs)
dsn_add_executable(${DSN_APP_TARGET} "${BINPLACE_FILES}")
